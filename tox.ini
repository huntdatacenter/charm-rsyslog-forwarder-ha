[tox]
skipsdist=True
envlist = py34, py35
skip_missing_interpreters = True

[testenv]
commands = py.test -v {posargs:tests/unit}
deps =
    -r{toxinidir}/test_requirements.txt
setenv =
    PYTHONPATH = {toxinidir}:{toxinidir}/hooks
# sitepackages needed for python-apt
sitepackages = True
